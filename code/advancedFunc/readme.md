# Advanced function with multiple argument intake and return multiple results
    1. func calculator(num1 float64, num2 float64)(float64,float64,float64,float64){}
       takes two input from user , and resturn 4 float64 results 
       that are:
         1. Sum
         2. Substraction
         3. Multiplication
         4. Division
        
        these are of return type arguments that the function will return
        at the end of function return sum, sub, mul, div makes aure
    2. The user input variable are defined in main(){} instead of custom function, and Sum, Sub, Mul, Div are also return i nthe same block makingit modulised.
    3. Division variable is value formated with after decimnal point of 4 by %.4f