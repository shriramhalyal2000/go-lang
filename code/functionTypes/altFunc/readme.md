# in this excersise, there us using multiple function user defined
1. The function getUserInput accepts string value as parameter but returns float ,
    as referenced it dispaolys "Revenue", but user inputs float64 handled by fmt.Scanln() to accept it 
    and same thing is returned.
2. Instead of repeating Println & scanlan a user input is defined to accept parametes within another function block