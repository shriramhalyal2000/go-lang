# this demonstrates go-lang file handling
- File open, write/read/ulter data, close
- File operation in go lang are carried out by os package, that has os level access toread , wriete, file, create , delete directories.

1. Reading file in go lang.
   - reading a text file of .txt format
   1. import "os" package in module and refer with os.functions.
   